import { View, Text, StyleSheet } from 'react-native';

const OverseasEmployment  = () => {
  return (
    <View style={styles.container}>
      <Text>해외에 관한 내용</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default OverseasEmployment  
